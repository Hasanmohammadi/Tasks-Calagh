export interface TasksType {
  id: string;
  isStart: boolean;
  title: string;
  project: string;
  description: string;
  createdTime: string;
  startedTime: string;
  finishedTime: string;
  taskStarted: number;
}
